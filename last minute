run.Execute()

#load results
app.ResLoadData(elmres)

#find column indices
time_index = app.ResGetIndex(elmres, elmres, "b:tnow")
freq_index = app.ResGetIndex(elmres, bus, "m:fehz")

#number of time steps
n = app.ResGetValueCount(elmres, 0)

time_vals = []
freq_vals = []

for i in range(n):
    t = app.ResGetData(elmres, i, time_index)[1]
    f = app.ResGetData(elmres, i, freq_index)[1]

    time_vals.append(t)
    freq_vals.append(f)
